name: ccodwg-update-phu

on:
  schedule:
    - cron: 0 2 * * *

jobs:
  run-update:
    runs-on: ubuntu-latest
    name: Run CCODWG update (phu)
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run commands in Docker container
        uses: ./actions/ccodwg-update-run
        with:
          command_string: "scripts/copy-service-key.sh; scripts/update-phu.sh"
          service_key: ${{ secrets.service_key }}
